# dotenv environment variable files
.env
.env.development.local
.env.test.local
.env.production.local
.env.local

# Fresh build directory
_fresh/
# npm dependencies
node_modules/

# Agent build output (200MB compiled binary — shipped via GitHub Releases)
dist/

# Dev-mode agent state created by scripts/dev_all.ts (per-developer, not shared)
.dev-agent/
